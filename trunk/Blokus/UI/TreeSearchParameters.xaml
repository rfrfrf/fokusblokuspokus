<UserControl x:Class="Blokus.UI.TreeSearchParameters"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label>Max głębokość poszukiwań:</Label>
        <TextBox Grid.Row="1" Text="{Binding MaxDepth}" />
        <Label Grid.Row="2">Max rząd drzewa:</Label>
        <TextBox Grid.Row="3" Text="{Binding MaxTreeRank}" />
    </Grid>
</UserControl>
